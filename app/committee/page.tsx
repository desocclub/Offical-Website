import CommitteePageContent from '@/components/committee/CommitteePageContent';

export const metadata = {
  title: 'Committee — DESOC',
  description: 'Meet the teams that shape DESOC across past, present, and future years.',
};

export default function CommitteePage() {
  return <CommitteePageContent />;
}

